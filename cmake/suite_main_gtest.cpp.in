// Generated from cmake/suite_main_gtest.cpp.in -- do not edit, and do not commit.
//
// The main() for a suite partway through conversion to GoogleTest. Conversion
// runs one source file at a time, so a suite holds both kinds at once: TEST()s,
// which RUN_ALL_TESTS finds on its own, and the entry points test/suites.cmake
// still lists, which have to be called by name. Those are wrapped in a single
// test case so that gtest reports a throw from one of them as a failure instead
// of letting it terminate the binary.
//
// When the last source in a directory is converted the block below disappears
// and this becomes an ordinary gtest main.

#include <gtest/gtest.h>

@IOV2_SUITE_DECLARATIONS@
@IOV2_SUITE_LEGACY_TEST@
int main(int argc, char** argv)
{
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
