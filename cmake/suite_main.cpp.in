// Generated from cmake/suite_main.cpp.in -- do not edit, and do not commit.
//
// One of these per test suite, replacing the part test/Makefile's seven
// aggregate main()s used to play. The entry points and their order come from
// test/suites.cmake, which is derived from those main()s, so a suite runs
// exactly the functions it ran before and in the same order. The try/catch is
// copied from them too: a test that throws has to be reported, not aborted.

#include <exception>
#include <string>

#include <support/dump_info.h>

@IOV2_SUITE_DECLARATIONS@
int main()
{
    try
    {
@IOV2_SUITE_CALLS@        return 0;
    }
    catch (const std::exception& e)
    {
        dump_info((std::string("\n[!] @IOV2_SUITE_NAME@ failed with exception: ")
                   + e.what() + "\n").c_str());
        return 1;
    }
    catch (...)
    {
        dump_info("\n[!] @IOV2_SUITE_NAME@ failed with an unknown exception.\n");
        return 1;
    }
}
